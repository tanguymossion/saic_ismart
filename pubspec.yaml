name: saic_ismart
description: Dart client for the SAIC iSmart API — MG, Roewe, Maxus/LDV connected vehicles.
version: 1.0.0
repository: https://github.com/tanguymossion/saic_ismart

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.2.0
  encrypt: ^5.0.3
  crypto: ^3.0.3
  convert: ^3.1.1

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0